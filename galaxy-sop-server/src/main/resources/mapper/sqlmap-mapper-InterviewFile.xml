<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.galaxyinternet.model.project.InterviewFile" >
					
  <resultMap id="BaseResultMap" type="com.galaxyinternet.model.project.InterviewFile" >
    <id column="id" property="id" jdbcType="BIGINT" />
    <result column="record_id" property="recordId" jdbcType="BIGINT" />
    <result column="document_id" property="documentId" jdbcType="BIGINT" />
  </resultMap>
  
  <sql id="Base_Column_List" >
    id, record_id, document_id
  </sql>
  
  	<sql id="Base_Where_Clause">
		<where>
			<trim prefixOverrides="and">
				<if test="id != null" > and id =#{id} </if>
      			<if test="recordId != null" > and record_id = #{recordId,jdbcType=BIGINT} </if>
      			<if test="documentId != null" > and document_id = #{documentId,jdbcType=BIGINT}  </if>
			</trim>
		</where>
	</sql>
	
	<!-- 查询总数 -->
	<select id="selectCount" resultType="java.lang.Long" parameterType="java.util.Map">
		select count(id) from sop_interview_file
		<include refid="Base_Where_Clause" />
	</select>

	<!-- 查询 -->
	<select id="select" resultMap="BaseResultMap" parameterType="java.util.Map">
		select
		<include refid="Base_Column_List" />
		from sop_interview_file
		<include refid="Base_Where_Clause" />
	</select>

	<!-- 根据ID查询 -->
	<select id="selectById" resultMap="BaseResultMap" parameterType="java.lang.Long">
		select
		<include refid="Base_Column_List" />
		from sop_interview_file where id = #{id}
	</select>
	
  	<insert id="insert" parameterType="com.galaxyinternet.model.project.InterviewFile" >
	    <![CDATA[
			insert into sop_interview_file (id, record_id, document_id
		      )
		    values (#{id,jdbcType=BIGINT}, #{recordId,jdbcType=BIGINT}, #{documentId,jdbcType=BIGINT}
		      )
	    ]]>
    </insert>
    
    
    <!-- 更新 -->
	<update id="updateByIdSelective" parameterType="com.galaxyinternet.model.project.InterviewFile">
		update sop_interview_file
		<set>
			<if test="recordId != null" > record_id = #{recordId,jdbcType=BIGINT}, </if>
      		<if test="documentId != null" > document_id = #{documentId,jdbcType=BIGINT} </if>
		</set>
		<where>
			<trim prefixOverrides="and">
				<if test="id != null"> and id =#{id,jdbcType=BIGINT}</if>
				<if test="recordId != null" > and record_id = #{recordId,jdbcType=BIGINT} </if>
      			<if test="documentId != null" > and document_id = #{documentId,jdbcType=BIGINT} </if>
			</trim>
		</where>
	</update>
	
    <!-- 通过ID更新 -->
	<update id="updateById" parameterType="com.galaxyinternet.model.project.InterviewFile">
		update sop_interview_file
		<set>
			<if test="recordId != null" >  record_id = #{recordId,jdbcType=BIGINT}, </if>
      		<if test="documentId != null" > document_id = #{documentId,jdbcType=BIGINT} </if>
		</set>
		where id = #{id}
	</update>
    
    <!-- 删除 -->
	<delete id="delete" parameterType="java.util.Map">
		delete from sop_interview_file
		<include refid="Base_Where_Clause" />
	</delete>
     <!-- 根据ID删除 -->
	<delete id="deleteById" parameterType="com.galaxyinternet.model.project.InterviewFile">
		delete from sop_interview_file where id = #{id}
	</delete>
	
	

</mapper>